{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check": "astro check",
    "preview": "astro preview",
    "pages:dev": "bun run build && wrangler pages dev dist",
    "pages:create": "wrangler pages project create calmd --production-branch main",
    "deploy:pages": "bun run build && wrangler pages deploy dist --project-name calmd",
    "deploy:preview": "bun run build && wrangler pages deploy dist --project-name calmd --branch preview",
    "astro": "astro"
  },
  "dependencies": {
    "@fontsource-variable/jetbrains-mono": "5.3.0",
    "astro": "^7.1.6"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "wrangler": "^4.116.0"
  }
}
